# frozen_string_literal: true

RSpec.describe Kamal::Podman do
  it "has a version number" do
    expect(Kamal::Podman::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
